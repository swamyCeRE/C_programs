#include <stdio.h>
#define swapnib(x,y)  (x^=y^=x^=y)
//void swapnib(int x,int y);
int main()
{
    int x=5,y=4,a;
     swapnib(x,y);
   printf("x=%d,y=%d",x,y);

    return 0;
}
/*void swapnib(int x,int y)
{
    (x^=y^=x^=y);
printf("x=%d,y=%d\n",x,y);
}*/



======================================


alternative program:

#include <stdio.h>

#define SWAMP(x,y) \
do{ int temp=x; x=y; y=temp; \
} while(0)


int main()
{
    int x=50,y=20;
    
    SWAMP(x,y);
    printf("%d,%d\n",x,y);
    return 0;
}
